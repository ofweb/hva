---
name: pypi
description: "Use first for Python package versions, metadata, release lookup, and exact package-name checks. Never guess versions."
---

# PyPI

Use `pypi_package_info` for Python package questions when the exact package name is known.

Use this for:

- latest package version
- package metadata
- release lookup
- "what version is current?"

Never guess Python package versions.

Use this before broad web search.
