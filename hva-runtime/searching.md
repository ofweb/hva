SEARCHING (mandatory):

- Use built-in tools before bash.
- Workspace text search: `ripgrep_search` first.
- File names, file counts, and directory listing: `ls` or `find` first.
- Do not use bash grep or bash find pipelines for that.
